<!-- SYNC: v0.382.0 · #452 · 2026-06-22 — utrzymywane przez `pnpm docs:check` (NIE edytuj ręcznie wersji bez aktualizacji statusu) -->
<div align="center">

# 🧩 FAZY PROJEKTU &nbsp;·&nbsp; E‑BOT

![Faza 0](https://img.shields.io/badge/Faza_0-✅-E50914?labelColor=0a0a0a)
![Faza 1](https://img.shields.io/badge/Faza_1-✅-E50914?labelColor=0a0a0a)
![Faza 2](https://img.shields.io/badge/Faza_2-✅-E50914?labelColor=0a0a0a)
![Faza 3](https://img.shields.io/badge/Faza_3-✅-E50914?labelColor=0a0a0a)
![Faza 4](https://img.shields.io/badge/Faza_4-✅-E50914?labelColor=0a0a0a)
![Faza 5](https://img.shields.io/badge/Faza_5-✅-E50914?labelColor=0a0a0a)
![Faza 6](https://img.shields.io/badge/Faza_6-✅-E50914?labelColor=0a0a0a)
![Faza 7](https://img.shields.io/badge/Faza_7-✅-E50914?labelColor=0a0a0a)
![Faza 8](https://img.shields.io/badge/Faza_8-✅-E50914?labelColor=0a0a0a)
<br/>
![Etapy A–K](https://img.shields.io/badge/Etapy_A–K-✅-E50914?labelColor=0a0a0a)
![Architekt serwera](https://img.shields.io/badge/Architekt_serwera-✅-E50914?labelColor=0a0a0a)
![Multi-serwer](https://img.shields.io/badge/Config_multi--serwer-✅-E50914?labelColor=0a0a0a)
![i18n bota](https://img.shields.io/badge/i18n_bota_14_jęz.-✅-E50914?labelColor=0a0a0a)
![i18n panelu](https://img.shields.io/badge/i18n_panelu_39%2F39-✅-E50914?labelColor=0a0a0a)
![Wersja](https://img.shields.io/badge/wersja-0.382.0-E50914?labelColor=0a0a0a)

</div>

> Status zadań — **aktualizowany na bieżąco** (`[x]` zrobione, `[ ]` w planie). Źródło prawdy dla numeracji/wersji: [`CHANGELOG.md`](../CHANGELOG.md).
> 🔄 **Ten plik + [`ROADMAP.md`](ROADMAP.md) są synchronizowane z CHANGELOG przy każdym update** — weryfikacja: `pnpm docs:check`. Zasada utrzymania: [`CLAUDE.md`](../CLAUDE.md).

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 🔭 Bieżący tor (v0.382.0)

**🌍🏁 i18n UI panelu — UKOŃCZONE** — etykiety i formularze **wszystkich** stron panelu przetłumaczone na **14 języków** (PL, EN, DE, ES, IT, FR, PT, ZH, KO, RU, UK, JA, AR + RTL, ID). Powłoka panelu, pomoc „Jak to działa?" (37/37), web GameVault oraz wszystkie strony ustawień — komplet.

- [x] **Zrobione 39/39 stron (komplet)**: `/` (Pulpit) `/welcome` `/modules` `/leaderboard` `/logging` `/audit` `/modmail` `/birthdays` `/suggestions` `/appearance` `/wishlist` `/notifications` `/live` `/tickets` `/security` `/levels` `/counters` `/responder` `/automations` `/scheduled` `/diagnostics` `/stats` `/profile` `/donations` `/economy` `/eco` `/library` `/integrations` `/gaming` `/creator` `/roles` `/commands` `/applications` `/ai` `/engagement` `/custom-commands` `/moderation` `/settings` `/setup`
- [x] Osobna, opcjonalna fala: wewnętrzne etykiety współdzielonego `CardStyleEditor` + `GradientField` (15 kluczy × 14 jęz.) ✅ **v0.249.0**
- [x] Strona główna (Pulpit `/`) + widgety + checklista modułów + `relTime` (Intl) — 53 klucze `ui.home.*` + 16 `ui.checklist.*` × 14 jęz. ✅ **v0.250.0**
- [x] Powierzchnia publiczna / pre-auth (osobno od 39/39): logowanie + publiczny ranking `/p/leaderboard` + publiczny profil `/p/u/[id]` (chrome + `generateMetadata`) — 17 kluczy `ui.pub.*` × 14 jęz. ✅ **v0.251.0**
- [x] Boilerplate frameworka: `error.tsx` + `not-found.tsx` (404) + `loading.tsx` (Suspense) + metadane `layout.tsx` — 8 kluczy `ui.sys.*` × 14 jęz. ✅ **v0.252.0**
- [x] Obraz OG profilu (`opengraph-image.tsx`): dynamiczne fonty Google per-skrypt (fail-safe, subset TTF) + etykiety — 5 kluczy `ui.og.*` × 14 jęz. ✅ **v0.253.0**. **KONIEC i18n CAŁEJ powierzchni web** — nie zostaje żaden niezlokalizowany element UI.
- [x] **Audyt i18n 14 jęz.** — parzystość **1394×14** (0 brakujących), 0 duplikatów, tokeny `{…}` 100% spójne; **naprawiony RTL** (arabski: `dir="rtl"` na `<html>` — SSR z cookie + klient na zmianę języka). ✅ **v0.254.0**
- [x] **RTL — KOMPLETNY** 🏁: chrom nawigacyjny (v0.255–256) + Pulpit/widgety (v0.257.0) + 14 stron `app/*` (v0.258.0) + 22 komponenty (v0.259.0) + knoby przełączników (v0.260.0). Cała powierzchnia na logicznych klasach Tailwind v4; finalny grep — zero fizycznych klas kierunkowych poza 2 wyśrodkowaniami. ✅

### 🧭 Otwarte / strategiczne (poza torem i18n)
- [ ] **Marketplace pluginów / multi-guild — 🚧 M1 ✓ · M2 ✓ · M3 ✓ · M4 ✓ · M5 ✓ · M6 ✓ (sandbox + auto-trigger)** — decyzje: ✅ **płatne** (tiery) + ✅ **community** (3rd-party) → pełny zakres **M1–M6**. **Zrobione:** schemat + multi-tenant + chokepoint izolacji (v0.267–269) + katalog + strona `/marketplace` + toggle (v0.270–272) + self-serve login [`lib/enroll.ts`](../dashboard/lib/enroll.ts) + **onboarding `/onboarding`** (dodaj bota + Twoje serwery, v0.273+279) + **billing Stripe** (v0.274–275; [`AKTYWACJA-STRIPE.md`](AKTYWACJA-STRIPE.md)) + community pipeline `/marketplace/submit`+`/review` (zgłoszenie→moderacja→katalog, v0.276–278) + **i18n WSZYSTKICH powierzchni ×14** (onboarding/review/submit, 25 kluczy, v0.280–281) + **M3 config pluginów** ([`lib/pluginConfig.ts`](../dashboard/lib/pluginConfig.ts): `plugin_config`=community; first-party bez migracji, v0.282.0) + **self-review bezpieczeństwa** (4 luki cross-tenant naprawione — export/staff/import/global-write, v0.283.0; [`SECURITY-REVIEW-MARKETPLACE.md`](SECURITY-REVIEW-MARKETPLACE.md)). **Sandbox wykonania obcego kodu — design gotowy** ([`PLAN-M6-SANDBOX.md`](PLAN-M6-SANDBOX.md): webhook-first + capability, fazy M6a–M6d; v0.284.0) → **M6a runner webhook** ([`lib/pluginRunner.ts`](../dashboard/lib/pluginRunner.ts): kontrakt + HMAC + SSRF-guard, v0.285.0) → **M6b wykonanie akcji** ([`lib/pluginExecutor.ts`](../dashboard/lib/pluginExecutor.ts) + [`lib/discordActions.ts`](../dashboard/lib/discordActions.ts): `setConfig` + `sendMessage`/`addRole` z per-akcja authz/anty-eskalacja — kanał/rola ∈ gildia, rola bez groźnych uprawnień, v0.286–287) + **M6c dry-run** ([`/api/community/dryrun`](../dashboard/app/api/community/dryrun/route.ts): owner-only test endpointu przez runner, **bez wykonania**, v0.288.0) + **trigger produkcyjny** ([`lib/pluginInvoke.ts`](../dashboard/lib/pluginInvoke.ts) + [`/api/community/run`](../dashboard/app/api/community/run/route.ts): orchestrator z 6 warstwami strażników + owner-triggered **realne wykonanie**, v0.289.0; auto-trigger na zdarzenia → most bota niżej) + formularz endpoint/secret (v0.290.0) + **toggle community per-serwer** ([`/api/community/toggle`](../dashboard/app/api/community/toggle/route.ts) → `guild_plugins`; **PEŁNA pętla UI ✓**, v0.291.0) + **auto-trigger z bota** (most [`bot/src/cloud/plugin-bridge.mts`](../bot/src/cloud/plugin-bridge.mts) → [`/api/internal/plugin-event`](../dashboard/app/api/internal/plugin-event/route.ts) → fan-out [`invokeGuildEvent`](../dashboard/lib/pluginInvoke.ts); `guildMemberAdd`/`Remove`/`Boost` + `messageCreate` (filtr słów-kluczy, manifest `keywords[]` + [`/api/internal/plugin-subscriptions`](../dashboard/app/api/internal/plugin-subscriptions/route.ts)) odpalają włączone+zatwierdzone pluginy przez audytowany sandbox; reakcje/voice świadomie pominięte (ochrona przed zalaniem); autor wybiera `event`+`keywords` w formularzu zgłoszeń (i18n ×14) — **„żywa" pętla ✓**, v0.292–295) + **przewodnik aktywacji** ([`AKTYWACJA-COMMUNITY.md`](AKTYWACJA-COMMUNITY.md): env + kontrakt webhooka + przykładowy plugin, v0.296.0). Plan: [`PLAN-MARKETPLACE.md`](PLAN-MARKETPLACE.md)
- [ ] **Retencja + więcej wykresów w czasie** (panel `/stats`) — przyrosty 1–3 ✅ **v0.261–263** (wzrost członków + komplet trendów + **konfigurowalny zakres 7/14/30/90d** + **eksport CSV**); **kohortowa retencja ✓** — fundament danych (tabela `member_cohorts` + tracking join/leave w bocie [`analytics/cohorts.mts`](../bot/src/analytics/cohorts.mts) z bounded backfillem 90 dni, v0.297.0) + **wykres D1/D7/D30 na `/stats`** ([`lib/retention.ts`](../dashboard/lib/retention.ts) eligible-based + sekcja UI + i18n ×14, v0.298.0) + **Audyt #2 bezpieczeństwa** — naprawa F5 (scoping analityki `/stats` przez `getPrimaryGuildId`, anty-przeciek cross-tenant; [`SECURITY-REVIEW-MARKETPLACE.md`](SECURITY-REVIEW-MARKETPLACE.md), v0.299.0) + **rezydua F5 domknięte** (`server_history` + `ai_usage` per-serwer (cała analityka `/stats` scoped chokepointem; v0.300–301)
- [ ] **Produkcyjne wpięcie infry** — szkielety **kompletne i gated** (audyt v0.265.0, przewodnik [`AKTYWACJA-INFRA.md`](AKTYWACJA-INFRA.md)): Sentry no-op bez `SENTRY_DSN`; Realtime z fallbackiem poll 60 s (aktywacja = `ALTER PUBLICATION … ADD TABLE settings`); Redis niewpięty (opcja na skalę). Aktywacja Sentry/Realtime wymaga **Twoich** zasobów
- [ ] **Twitch sub → rola** — kod **kompletny + gotowy do aktywacji** (v0.264.0): runtime obsługuje `channel.subscribe`→rola, `eventsub-setup.mts` rejestruje obie subskrypcje, przewodnik [`AKTYWACJA-TWITCH-SUB.md`](AKTYWACJA-TWITCH-SUB.md). Aktywacja czeka na **Twoje** zasoby: aplikacja Twitch + OAuth broadcastera (scope `channel:read:subscriptions`)
- [ ] **Sharding (skala >2500 serwerów)** — bot **shard-ready** (v0.302.0): audyt shard-safety + uodpornione globalne agregaty (`heartbeat` sumuje `broadcastEval`/pisze tylko shard 0; pollery tempban/temp-role pomijają obce serwery), opcjonalny [`shard.mts`](../bot/src/shard.mts) (`ShardingManager`, skrypt `shard`); przewodnik [`SHARDING.md`](SHARDING.md). Włączasz przy zbliżaniu się do progu
- [ ] **Hardening po audycie całości (P0→P3)** — pełny **5-wymiarowy audyt** (bezpieczeństwo · zależności · kompletność · UX/wydajność · jakość/testy; werdykt: **0 podatności zależności, 0 TODO/stub, type-safety wzorowa**). **P0 w toku:** nagłówki bezpieczeństwa HTTP (CSP/HSTS/X-Frame/nosniff) w panelu + GameVault ✅ **v0.308.0** + anty-eskalacja `/roleperms`+`/rolecopy` ✅ **v0.308.0** + ujednolicenie uprawnień bota → Administrator ✅ **v0.309.0** + sekret admina (fail-closed) na `web/ /api/settings` ✅ **v0.309.0**; **bramka instance-admin na sekretach globalnych (Ko-fi + webhook-relay)** ✅ **v0.310.0** (reszta globalnych tras: ai_config/integrations/social_feeds/setup = kolejna partia) + **testy rdzenia bezpieczeństwa (20 vitest) + E2E Playwright w CI** ✅ **v0.311.0**. **Priorytety P0+P1 z audytu domknięte**; **„ogon" #2 — 4 globalne configi (integrations/ai-config/locale/presence) zabramkowane** ✅ **v0.312.0**; zostają trasy per-serwer do konwersji (setup/social_feeds/scheduled/creator) + **observability: błędy serwera → Sentry (onRequestError)** ✅ **v0.313.0** + **perf: edge-cache `/api/img`** ✅ **v0.314.0** + **UX/perf: GuildSwitcher `router.refresh` + `loading.tsx` serwerowy** ✅ **v0.315.0** + **a11y: prymityw `useFocusTrap` + dialog `MobileNav`** ✅ **v0.316.0** + **`CommandPalette` (dialog)** ✅ **v0.317.0**. **Re-audyt** (głębszy) → **IDOR shop/tickets domknięty** ✅ **v0.318.0**; **Sentry-w-catch (billing+auth)** ✅ **v0.319.0**; **web/ resilience — `safeGenres` (uszkodzony wiersz nie ubija strony) + granice błędu `error.tsx`/`global-error.tsx`** ✅ **v0.320.0**; **SSRF IPv4-mapped IPv6 (`::ffff:*` dotted+hex, `::`) + `CRON_SECRET` timing-safe (`timingSafeEqual`)** ✅ **v0.321.0**; **zod (`safeParse`) na globalnym configu `integrations`** ✅ **v0.322.0** → 🏁 **tier P0 z re-audytu domknięty**; **P1 w toku:** rate-limit publicznego sinku `/api/sentry` (10/min per IP + cap 16 KB, wspólny `lib/rateLimit.ts`, `/api/hook` zdedup.) ✅ **v0.323.0**; `engines` (Node ≥24 · pnpm ≥11) w 5 pakietach ✅ **v0.324.0**; `confirm()` na nieodwracalnych usunięciach (wishlist + sklep) ✅ **v0.325.0**; testy izolacji multi-tenant (`isolation.test.ts` — mock Supabase, rygiel anty-IDOR, 6 testów) ✅ **v0.326.0**; embla RTL (GameVault) + fundament `images.remotePatterns` (oba configi) ✅ **v0.327.0** → 🏁 **tier P1 z re-audytu domknięty**; **P2/P3 w toku:** kosmetyka docs (spójność „59 usług", martwe komentarze i18n, dedup README) ✅ **v0.328.0**; kontrast WCAG AA na 12 etykietach panelu (`text-muted`/białe przezroczystości → solidne) ✅ **v0.329.0**; **fix: modal GameVault się nie zamykał (`AnimatePresence` + React 19) + a11y dialogu (`useFocusTrap`→web/, Escape/focus-trap)** ✅ **v0.330.0**; **a11y klawiatury dropdownu języka (Escape + restore) w GameVault** ✅ **v0.331.0**; **focus-trap/Escape na overlayach panelu (GameDetailModal trap; Assistant/TourGuide tylko Escape — non-modal)** ✅ **v0.332.0**; **parytet hardeningu `/api/img` web/↔panel (timeout 8s + edge-cache, SSRF bez zmian)** ✅ **v0.333.0**. **migracja globalnych configów → per-serwer — START wzorcem:** `aimod_config` (singleton w bocie → `cfgFor` per-guild + `getConfigSetting`/`MIGRATED_GUILD_KEYS` w panelu) ✅ **v0.334.0**. Mechanizm okazał się **nisko-ryzykowny** (`getGuildSettings` ma fallback per-serwer→globalny ⇒ brak utraty danych; błędne okablowanie degraduje do globalnego, nie do breakage), ale **runtime-niewryfikowalny bez żywego multi-guild bota**. `aihelp_config` ✅ **v0.335.0** (handler). **`aidigest`** ✅ **v0.336.0** (wzorzec pollera, przetestowany). **3 gaming-feedy** ✅ **v0.337.0** (`pricetracker` naprawił przeciek wishlisty). **Ostatnie 3 pollery** (`social_feeds`/`scheduled_posts`/`creator`) ✅ **v0.338.0** → 🏁 **KONIEC migracji 9/9** globalnych configów na per-serwer (config z fallbackiem globalnym + dedup per-serwer + izolacja kanałów `guild.channels.fetch`). Runtime-niewryfikowalne tutaj — do testu feedów/postów/klipów na ≥2 serwerach; **rdzeń ODCZYTU configu per-serwer zaryglowany** testem [`db.isolation.test.ts`](../bot/src/lib/db.isolation.test.ts) (8× — override `g:<id>:` > globalny fallback > **anty-leak B→A**; `configWriteKey`; mutacja merge'a zwala 5/8 = dowód, że gryzie) ✅ **v0.341.0** — komplementarny do panelowego [`isolation.test.ts`](../dashboard/lib/isolation.test.ts). **Runtime pollera zaryglowany wzorcowo** — [`freegames.isolation.test.ts`](../bot/src/gaming/freegames.isolation.test.ts) (4× — routing per-serwer przez `guild.channels.fetch` + `enabled:false`→cisza + dedup `g:<id>:freegames_seen`; mutacja na globalny dedup zwala 2/4) ✅ **v0.342.0** + [`pricetracker.isolation.test.ts`](../bot/src/gaming/pricetracker.isolation.test.ts) (4× — **scope `guild_id` na wishliście** = drugi wektor: zapytanie DB, nie dedup; rygluje przeciek z v0.337; mutacja na globalne zwala 3/4) ✅ **v0.343.0** + [`scheduledPosts.isolation.test.ts`](../bot/src/engagement/scheduledPosts.isolation.test.ts) (8× — **logika harmonogramu** `dueNow`: tryby once/daily/weekly + okno 10 min + **strefa Europe/Warsaw+DST** + anty-duplikat dzienny, oraz izolacja state per-serwer; mutacja strefy→UTC zwala 4, klucza→globalny zwala izolację) ✅ **v0.344.0** + ostatnie 4 pollery [`aidigest`](../bot/src/community/aidigest.isolation.test.ts)/[`social`](../bot/src/creator/social.isolation.test.ts)/[`clips`](../bot/src/creator/clips.isolation.test.ts)/[`patchnotes`](../bot/src/gaming/patchnotes.isolation.test.ts) (anty-spam seed · bramka godziny UTC · **dedup GLOBALNY udokumentowany** dla clips) ✅ **v0.345.0** → 🏁 **KOMPLET izolacji 7/7 pollerów** + rdzeń `db.mts` + logika `scheduledPosts`. Dołożony **rygiel spójności cross-package `MIGRATED_GUILD_KEYS` bot⊆panel** ([`migrated-keys-consistency.test.ts`](../bot/src/lib/migrated-keys-consistency.test.ts) — anty-rozjazd round-tripu zapisu: panel pisze per-serwer wszystko, co bot trzyma per-serwer; mutacja zwala RYGIEL) ✅ **v0.346.0**. **Nowy tor — rygle logiki bezpieczeństwa:** scoring anty-spam `heat` ([`heat.test.ts`](../bot/src/security/heat.test.ts) — wagi czynników `messageHeat`: powtórzenie/wzmianki/`@everyone`/emoji/link/długość; mutacja wagi zwala test) ✅ **v0.347.0** + detekcja fali anti-raid (`detectWave` ostrożnie wydzielona z handlera `GuildMemberAdd` — okno przesuwne + próg ≥; mutacja off-by-one zwala test) ✅ **v0.348.0** + scalanie configu anti-nuke (`mergeConfig` — deep-merge ochron + anty-wstrzyknięcie nieznanych kluczy; mutacja zwala test) ✅ **v0.349.0** → 🏁 **KONIEC toru bezpieczeństwa** (heat · antiraid · antinuke). Dalej rygle czystej logiki: krzywa XP→poziom `levelForXp` ([`leveling.test.ts`](../bot/src/leveling.test.ts) — progi kumulacyjne `5L²+50L+100` + monotoniczność; mutacja off-by-one zwala test) ✅ **v0.350.0** + progi odznak-tierów `tierAtLevel`/`nextTier` ([`achievements.test.ts`](../bot/src/lib/achievements.test.ts) — dokładny próg vs spam co poziom; mutacja `===`→`<=` zwala test) ✅ **v0.351.0** + matematyka ekonomii eko 2.0 (giełda `priceAt`/`changePct` · pety `petLevel`/`fullness`/`bar`/`moodKey` · format `fmt`) ([`economy-math.test.ts`](../bot/src/economy/economy-math.test.ts) — bundla 3 podsystemy; mutacja amplitudy giełdy zwala test) ✅ **v0.352.0** + **parytet i18n bota 14 jęz.** ([`parity.test.ts`](../bot/src/i18n/parity.test.ts) — każdy klucz bazy w 14 językach, udokumentowany wyjątek `card.*` dla nie-łacińskich; **przy okazji naprawiony realny brak `error.generic` w 12 językach** → [`strings.errors.mts`](../bot/src/i18n/strings.errors.mts)) ✅ **v0.353.0** + **parytet i18n panelu** ([`panelI18n.parity.test.ts`](../dashboard/lib/panelI18n.parity.test.ts) — UI **1430 × 14 jęz.** + MODES; pomiar 0 dryfu, zamrożone) ✅ **v0.354.0** → **oba słowniki i18n (bot 477 + panel 1430) zaryglowane** + treść „Jak to działa?" ([`howItWorksI18n.parity.test.ts`](../dashboard/lib/howItWorksI18n.parity.test.ts) — 37 stron × 13 tłumaczeń vs źródło pl, 0 dryfu) ✅ **v0.355.0** → 🏁 **parytet i18n CAŁEGO projektu zaryglowany**. Suite **26 plików / 220 testów** (start sesji 8/74). **Pełny audyt funkcjonalny** (komendy · usługi · configi · trasy API · ingest, 4 sondy) — werdykt: kod kompletny (95 slash + 3 context-menu spięte / 117 subkomend · 57/57 usług w tle · 93 trasy API · 0 TODO/stub · `MIGRATED_GUILD_KEYS` bot⊆panel); przy okazji naprawiona zepsuta bramka `pnpm typecheck` (root `exec`→`run` + skrypty `typecheck` w dashboard/web → pokrycie 3 pakietów, exit 0) ✅ **v0.356.0**. **Domknięcie znalezisk audytu:** `ingest` otypowany ([`tsconfig.json`](../ingest/tsconfig.json) wzorem bota + skrypt `typecheck` + devDeps `typescript`/`@types/node`) → `pnpm typecheck` obejmuje teraz **4 pakiety** (bot·dashboard·web·ingest, kolektory **type-clean**); `pnpm lint` exit 0 (auto-naprawialny drift `biome --write` zacommitowany: 6× `ingest/` + migracja glob `biome.json` + `next-env.d.ts` auto-gen → ignore); znalezisko #2 `twitch_sub`/`kofi` zweryfikowane jako **poprawne** (oba toggle bramkują webhook panelu `/api/kofi`+`/api/twitch/eventsub`) → 🏁 **4/4 znaleziska audytu domknięte** ✅ **v0.357.0**. **Rygiel logiki małżeństw (/marry)** ([`marriage.test.ts`](../bot/src/lib/marriage.test.ts) — 8× nad nietestowanym magazynem symetrycznym [`marriage.mts`](../bot/src/lib/marriage.mts)): zapis symetryczny (A→B i B→A z **identycznym** `since`) + **strażnik lustra przy rozwodzie** (`clearMarriage` kasuje lustro partnera tylko gdy ten nadal wskazuje na rozwodzącego się — ochrona świeżego związku po re-marriage: scenariusz A–B → B–C → rozwód A NIE rusza B–C) + izolacja multi-tenant; mutacja zdejmująca strażnika (`delete` bezwarunkowy) zwala RYGIEL re-marriage, rozjazd `since` zwala symetrię; 0 zmian produkcyjnych, realny SQLite ✅ **v0.358.0** → suite **27 plików / 228 testów**. **Rygiel blackjacka** ([`blackjack.test.ts`](../bot/src/economy/blackjack.test.ts) — 10× nad matematyką gry na pieniądze): **miękki as** (`val` — as 11→1 gdy ręka >21, pojedynczo i tylko ile trzeba: `A+K=21` natural, `A+A=12`, `A+A+A=13`, `A+A+9=21`, 4 asy=14, twardy bust) + **integralność talii** (`freshDeck` — 52 karty, 4×13, 0 duplikatów, suma 380 deterministyczna mimo tasowania); mutacja `total-=10`→`-=11` zwala 3 testy asów, podmiana rangi w builderze zwala integralność; zmiana produkcyjna = tylko `export` na `val`/`freshDeck`/`Card` (3 linie) ✅ **v0.359.0** → suite **28 plików / 238 testów**. **Rygiel TTL efektów itemów** ([`effects.test.ts`](../bot/src/economy/effects.test.ts) — 11× nad czystą logiką w pamięci [`effects.mts`](../bot/src/economy/effects.mts): XP×2 + tarcza anty-rabunek): **granica wygaśnięcia `exp < now` (ścisłe `<`)** — now===exp jeszcze działa, +1 ms gaśnie + leniwie usuwany; mnożnik `xpMultiplier` = 2 tylko dla `xp2`; **izolacja klucza `guild:user:effect`** (brak przecieku między userami/serwerami/typami); czas sterowany fałszywym zegarem `vi` (deterministyczne); mutacja `<`→`<=` zwala granicę, `xp2`→`shield` zwala mnożnik; 0 zmian produkcyjnych ✅ **v0.360.0** → suite **29 plików / 249 testów**. **Rygiel progów odznak** ([`badges.test.ts`](../bot/src/community/badges.test.ts) — 19× nad czystymi predykatami `check(stats)` [`badges.mts`](../bot/src/community/badges.mts); odznaki **permanentne** w `user_badges` → off-by-one = na zawsze): **13/13 progów `>=`** data-driven (poziom 5/10/25/50/100 · prestiż · majątek 10k/100k · streak 7/30 · zaproszenia 5/25 · gry 10 — dokładnie na progu wpada, niżej nie, monotoniczność w górę) + **izolacja wymiaru** (każdy predykat patrzy tylko na swoje pole) + anti-rozjazd test↔produkcja (tabela = komplet `BADGES`); mutacja `>=`→`>` zwala granicę, podmiana pola w predykacie zwala próg + izolację; 0 zmian produkcyjnych ✅ **v0.361.0** → suite **30 plików / 268 testów**. **Rygiel katalogu skórek** ([`skins.test.ts`](../bot/src/economy/skins.test.ts) — 9× nad czystymi danymi [`skins.mts`](../bot/src/economy/skins.mts); skórka nadpisuje styl `/rank`+`/profile`): **cross-module spójność fontu** — każda `style.font` ∈ `CARD_FONTS` z [`cards.mts`](../bot/src/lib/cards.mts), inaczej renderer `safeFont` **cicho** podmienia na Poppins (user płaci i nie dostaje fontu) + integralność katalogu (5 skórek, `classic` darmowa/jedyna, drabina cen `[0,5000,10000,15000,25000]`) + walidacja stylu (hex `#RRGGBB`, angle ∈ [0,360)) + `skinById` round-trip; mutacja fontu spoza `CARD_FONTS` zwala RYGIEL cross-module, `classic.price` 0→100 zauważa drabinę; 0 zmian produkcyjnych ✅ **v0.362.0** → suite **31 plików / 277 testów**. **Rygiel warstwy danych eko** ([`store-config.test.ts`](../bot/src/economy/store-config.test.ts) — 13× nad fundamentem [`store.mts`](../bot/src/economy/store.mts)): **`minutesSince`** — rdzeń KAŻDEJ bramki czasowej (`/eco work`/`rob`/`daily`): `null`→`+Infinity`, dzielnik `60_000` (60 s→1 min), przyszłość→ujemne (fałszywy zegar `vi`) + **`ecoConfig`** per-serwer override+fallback global (Etap K — merge płytki, uszkodzony JSON→`ECO_DEFAULT` bez wyjątku, świeża kopia, izolacja A↛B, realny SQLite) + **sanity `ECO_DEFAULT`** (`workMin≤workMax`, procenty ∈ [0,100], `gambleMax>0`); mutacja dzielnika `60_000`→`1_000` zwala cooldown, odwrócenie spreadu merge'a zwala override/izolację/fallback; 0 zmian produkcyjnych ✅ **v0.363.0** → suite **32 plików / 290 testów**. **Rygiel losowania giveawayów** ([`giveaways.test.ts`](../bot/src/engagement/giveaways.test.ts) — 7× nad ważonym losowaniem [`weightedPick`](../bot/src/engagement/giveaways.mts); niezmienniki dla **dowolnego** tasowania `Math.random` → pętla 200 przebiegów): dokładnie `n` zwycięzców **bez duplikatów** (brak podwójnej nagrody), nie więcej niż uczestników, każdy z puli, brak uczestników → `[]` + **dolny clamp wagi `Math.max(1,…)`** (waga 0/ujemna/brak pola nadal uprawniona — wpada gdy `n ≥ liczba osób`); mutacja usuwająca dedup zwala unikatowość, usuwająca clamp zwala 2 testy uprawnień; 0 zmian produkcyjnych ✅ **v0.364.0** → suite **33 plików / 297 testów**. **Rygiel toggle-parserów bram funkcji** ([`feature-toggles.test.ts`](../bot/src/community/feature-toggles.test.ts) — 14× = 7×2 parsery [`afkEnabled`](../bot/src/community/afk.mts)/[`highlightsEnabled`](../bot/src/community/highlights.mts)): **FAIL-SAFE OFF** — brak configu / **uszkodzony JSON** / brak pola `enabled` → `false` (nigdy wyjątek wywalający handler, nigdy przypadkowe włączenie) + override per-serwer + izolacja A↛B + fallback global (Etap K, realny SQLite); mutacja `catch → return true` zwala fail-safe, negacja koercji `!!x`→`!x` zwala 5 testów `enabled`; 0 zmian produkcyjnych ✅ **v0.365.0** → suite **34 plików / 311 testów**. **DRY krzywej levelingu — dedup 5 → 1** (przy okazji ryglowania): formuła `5L²+50L+100` była skopiowana niezależnie w 5 miejscach (`leveling`+`rank`+`profile`+`prestige`+`giveaways`) — każda kopia mogła dryfować ⇒ `/rank` poziom 5 vs `/prestige` 6. [`leveling.mts`](../bot/src/leveling.mts) eksportuje teraz `levelInfo(xp)` (`{level,xpInto,xpFor}`) jako **jedyne źródło prawdy**; usunięto 4 lokalne kopie (−34 linie netto, **0 zmian zachowania** — formuły bajt-w-bajt identyczne, potwierdzone typecheck+suite); rygiel `levelInfo` (+4 testy: `xpFor` progi 100/155/220, `xpInto` w poziomie, niezmiennik `0≤xpInto<xpFor`; mutacja `xp−acc+1` zwala) ✅ **v0.366.0** → suite **34 plików / 315 testów**. **Rygiel dopasowania autorespondera** ([`responder.test.ts`](../bot/src/community/responder.test.ts) — 6× nad wydzieloną czystą funkcją `matchTrigger` w [`responder.mts`](../bot/src/community/responder.mts); autoresponder reaguje na KAŻDĄ wiadomość → błąd = spam): refactor behavior-preserving (logika `exact`/`starts`/`contains` z handlera → eksportowana funkcja); zaryglowane: `exact` = **ścisła równość, nie podłańcuch** (`'ping pong'` exact `'ping'`→false), `starts` (prefiks), case-insensitive, **pusty trigger nigdy nie pasuje** (anti-spam — `contains ''` łapałby wszystko); mutacja `exact`→`includes` zwala ścisłość, usunięcie `if(!t)` zwala anti-spam ✅ **v0.367.0** → suite **35 plików / 321 testów**. **Rygiel rejestru języków** ([`locales.test.ts`](../bot/src/i18n/locales.test.ts) — 11× nad fundamentem routingu i18n [`locales.mts`](../bot/src/i18n/locales.mts)): **dwukierunkowa spójność map Discord↔Locale** (anti-drift — `LOCALE_TO_DISCORD` do rejestracji localizations vs `DISCORD_TO_LOCALE` dla `interaction.locale`; rozjazd ⇒ komenda pod kodem X rozwiązuje się na inny język; oba kierunki sprawdzone, wyjątek `ar=[]` Discord bez UI) + `isLocale` + `isRtl` (dokładnie 1 RTL=`ar`) + `fromDiscordLocale` (warianty `en-US`/`pt-BR`/`zh-CN`→baza) + kompletność `LOCALE_LABELS`; mutacja `pt-BR`→`en` zwala 3 testy spójności, `pl` w RTL zwala isRtl; zmiana produkcyjna = 1× `export DISCORD_TO_LOCALE` ✅ **v0.368.0** → suite **36 plików / 332 testy**. **Rygiel kart kolekcjonerskich (gacha)** ([`cards.test.ts`](../bot/src/economy/cards.test.ts) — 12× nad czystą logiką [`cards.mts`](../bot/src/economy/cards.mts), eko 2.0): drabina rzadkości `RARITY` (waga **maleje** common 50→mythic 1, sprzedaż **rośnie** 50→10000, suma wag 100) + integralność `CARDS` (unikalne id, **każda rzadkość ma ≥1 kartę** — inaczej `drawCard` spada na fallback) + `findCard` (case-insensitive+trim) + `rarityRank` (kolejność sortowania mythic<legendary<epic<rare<common) + `drawCard` (niezmienniki dla dowolnego `Math.random`: zawsze karta z `CARDS`, wszystkie 5 rzadkości osiągalnych); mutacja wagi legendary 5→20 zwala drabinę, odwrócenie `RARITY_ORDER` zwala `rarityRank`; 0 zmian produkcyjnych ✅ **v0.369.0** → suite **37 plików / 344 testy**. **Rygiel runtime'u i18n** ([`t.test.ts`](../bot/src/i18n/t.test.ts) — 9× nad funkcją `t()` w [`index.mts`](../bot/src/i18n/index.mts); KAŻDY zlokalizowany string przez nią przechodzi): interpolacja `{placeholder}` (wszystkie wystąpienia `/g`, nieznana zmienna **dosłownie**, liczby koercjonowane, bez vars surowo, tylko `{\w+}`) + **łańcuch fallback** `locale→en→pl→sam klucz` (nigdy `undefined`, nieobsługiwane locale bez wybuchu); interpolacja testowana na nieistniejącym kluczu (raw=klucz) ⇒ deterministyczne; mutacja `: m`→`: ''` zwala dosłowność placeholdera, `?? key`→`?? ''` zwala 7 testów; 0 zmian produkcyjnych ✅ **v0.370.0** → suite **38 plików / 353 testy**. **Rygiel ekonomii petów** ([`pets-gift.test.ts`](../bot/src/economy/pets-gift.test.ts) — 9× nad częściami nieobjętymi przez economy-math): **`giftValue`** (dochód pet = `giftBase × poziom × współczynnik sytości` 0.2–1.0; pełna sytość = giftBase×poziom, głód → factor 0.2, skaluje poziomem+sytością, ≥1; fałszywy zegar `vi` → deterministyczne) + **kap `petLevel` MAX_LEVEL=50** (ogromne XP nie przebija) + katalog `SPECIES` (unikalne id, adopt/giftBase >0) + `findSpecies` (case-insensitive+trim); mutacja factor `0.2`→`0.5` zwala 3 testy, usunięcie `Math.min(MAX_LEVEL,…)` zwala kap; 0 zmian produkcyjnych ✅ **v0.371.0** → suite **39 plików / 362 testy**. **Rygiel renderowania pożegnań/boostu** ([`farewell.test.ts`](../bot/src/farewell.test.ts) — 11× nad user-facing ścieżką [`farewell.mts`](../bot/src/farewell.mts): szablon z panelu + zmienne członka → embed): `renderVars` (literalne `split/join` = replaceAll — **wszystkie** wystąpienia nie tylko pierwsze, odporne na znaki regex w treści, pusta mapa→bez zmian) + `memberVars` (4 zmienne, format wzmianki `<@id>`, brak user→fallback `"user"`, pełna ścieżka `renderVars(memberVars)`) + `farewellEmbed` (kolor/opis, thumbnail tylko z URL); mutacja `split/join`→`replace` zwala test „wszystkie wystąpienia", zmiana fallbacku zwala test braku usera; 0 zmian produkcyjnych ✅ **v0.372.0** → suite **40 plików / 373 testy**. **Rygiel diagnozy uprawnień anti-nuke** ([`antinuke-perms.test.ts`](../bot/src/security/antinuke-perms.test.ts) — 5× nad `missingPerms` w [`antinuke.mts`](../bot/src/security/antinuke.mts)): `/antinuke status` musi pokazać REALNE braki uprawnień, by bot mógł bronić serwera — regresja = raport „OK" przy braku możliwości banowania/timeoutu (cicha dziura w obronie); zaryglowane: brak `me`→`["nieznane"]`, komplet 5→`[]`, mapowanie flaga→etykieta + kolejność (ViewAuditLog/Ban/Kick/Moderate/ManageRoles), zero uprawnień→komplet 5 braków; mutacja flip `!has`→`has` zwala 4 testy, zmiana etykiety zwala 2; 0 zmian produkcyjnych ✅ **v0.373.0** → suite **41 plików / 378 testów**. **Rygiel menu ról** ([`rolemenu.test.ts`](../bot/src/engagement/rolemenu.test.ts) — 8× nad `roleMenuConfig`+`buildRoleMenu` w [`rolemenu.mts`](../bot/src/engagement/rolemenu.mts)): **twarde limity Discorda** dla select-menu (przekroczenie = API odrzuca publikację `/rolemenu`): **kap 25 opcji**, label ≤100 / placeholder ≤150 / description ≤100; filtr opcji bez `roleId`, domyślne + uszkodzony JSON, izolacja per-serwer (realny SQLite), `buildRoleMenu` null gdy pusto + `maxValues=liczba opcji`; mutacja `.slice(0,25)` usunięta zwala kap, `placeholder.slice(0,150)` usunięta zwala przycięcie; 0 zmian produkcyjnych ✅ **v0.374.0** → suite **42 pliki / 386 testów**. **Rygiel rate-limitu publicznych sinków (panel)** ([`rateLimit.test.ts`](../dashboard/lib/rateLimit.test.ts) — 8× nad sliding-window [`rateLimit.ts`](../dashboard/lib/rateLimit.ts) chroniącym `/api/sentry`+`/api/hook` przed floodem; **pierwszy rygiel czystej logiki panelu poza schematami/i18n/izolacją**): `rateLimited` — off-by-one limitu (pierwsze `limit` ok, `+1` blok), reset po `windowMs`, **ścisła granica okna `now-t<windowMs`**, izolacja kluczy; `clientIp` (XFF pierwszy token→`x-real-ip`→`unknown`); fałszywy zegar `vi` → deterministyczne; mutacja `>`→`>=` zwala 4 testy (blokada legalnego ruchu), `<`→`<=` zwala 2 (okno nie domyka → DoS); 0 zmian produkcyjnych ✅ **v0.375.0** → suite **43 pliki / 394 testy**. **Rygiel spójności stylu kart bot↔panel** ([`cardstyle-consistency.test.ts`](../bot/src/lib/cardstyle-consistency.test.ts) — 4× cross-package [`cards.mts`](../bot/src/lib/cards.mts) ↔ [`cardStyle.ts`](../dashboard/lib/cardStyle.ts), wzorzec jak migrated-keys): `cardStyle.ts` to świadomie zduplikowana client-safe kopia (bez canvas), więc TS nie pilnuje zgodności — niezmiennik **oferta panelu == możliwości bota** (lista `CARD_FONTS` identyczna + domyślny `RANKCARD_DEFAULT`==`CARD_STYLE_DEFAULT` + font default ∈ CARD_FONTS); rozjazd ⇒ panel proponuje font, którego bot nie ma → cichy fallback na Poppins; mutacja `angle` 135→90 zwala default, usunięcie fontu z listy panelu zwala porównanie list; 0 zmian produkcyjnych ✅ **v0.376.0** → suite **44 pliki / 398 testów**. **Rygiel proxowania okładek** ([`cover.test.ts`](../dashboard/lib/cover.test.ts) — 8× nad czystą logiką [`cover.ts`](../dashboard/lib/cover.ts), GameVault/wishlist): `proxied` decyduje o routingu przez proxy `/api/img` (strażnik SSRF) — `data:` URI **bez proxy** (inline, proxowanie by je zepsuło), http(s) → `/api/img?u=<encodeURIComponent>` (znaki `?`/`&`/spacja zakodowane = brak wstrzyknięcia do query); `coverFallbacks` — uporządkowany łańcuch `cover→steam 600x900→steam header→placeholder`, zawsze placeholder na końcu, dedup `Set`; mutacja usuwająca `data:` passthrough zwala test inline, usunięcie `PLACEHOLDER` zwala 4 testy łańcucha; 0 zmian produkcyjnych ✅ **v0.377.0** → suite **45 plików / 406 testów**. **Rygiel listy języków bota w panelu** ([`botLocales.test.ts`](../dashboard/lib/botLocales.test.ts) — 5× nad [`botLocales.ts`](../dashboard/lib/botLocales.ts), świadomym mirrorem [`locales.mts`](../bot/src/i18n/locales.mts)): `normalizeBotLocale` (fallback `auto` dla nieznanej/null/pustej) + **spójność cross-package** — opcje panelu bez `auto` == zbiór `LOCALES` bota (14), `auto` obecne; rozjazd ⇒ panel proponuje język, którego bot nie obsługuje, albo gubi nowy; bot czytany jako tekst (jak migrated-keys — dashboard tsc nie importuje `.mts` z bot/); mutacja usuwająca język z opcji zwala równość zbiorów, usunięcie `.includes` zwala fallback; 0 zmian produkcyjnych ✅ **v0.378.0** → suite **46 plików / 411 testów**. **Rygiel presetów motywu** ([`themes.test.ts`](../dashboard/lib/themes.test.ts) — 4× nad [`themes.ts`](../dashboard/lib/themes.ts)): **kontrakt CSS** — `rgb`/`hover`/`dark` zasilają `rgb(var(--accent-rgb) / alpha)`, więc MUSZĄ być tripletem `"R G B"` (spacje, kanały 0–255); przecinek lub >255 = cicho zepsuty akcent CAŁEGO panelu; + integralność (unikalne id, niepuste nazwy) + helper `isRgbTriple` z samokontrolą; mutacja `rgb` na `229,9,20` zwala kontrakt, duplikacja `id` zwala unikalność; 0 zmian produkcyjnych ✅ **v0.379.0** → suite **47 plików / 415 testów**. **Rygiel katalogu giełdy** ([`stocks-catalog.test.ts`](../bot/src/economy/stocks-catalog.test.ts) — 6× uzupełnia economy-math, który zaryglował priceAt/changePct tylko dla GHOST): `findStock` (case-insensitive+trim) + **kontrakt uppercase** (`findStock` UPPERCASE'uje wejście ⇒ każdy symbol `STOCKS` MUSI być wielką literą, inaczej spółka niezznajdowalna — kup/sprzedaj nie działa; test: uppercase + round-trip) + integralność (unikalne symbole, base/vol >0) + **pasmo cen per-spółka** `base × [max(0.15,1−0.38·vol), 1+0.38·vol]` (sweep 400 pkt, łapie regresję vol/amplitud poza GHOST); mutacja `.toUpperCase()` usunięta zwala case-insensitive, symbol małymi literami zwala kontrakt+lookup; 0 zmian produkcyjnych ✅ **v0.380.0** → suite **48 plików / 421 testów**. **DRY helpera cooldownu — dedup 3 → 1** (przy okazji ryglowania): formuła `minutesSince` (`null→+∞` / `(Date.now()−Date.parse)/60_000`) była w 3 kopiach ([`store.mts`](../bot/src/economy/store.mts) — testowane, [`cards.mts`](../bot/src/economy/cards.mts) + [`pets.mts`](../bot/src/economy/pets.mts) jako `minutesSinceIso`); cards/pets **re-eksportują** `minutesSince as minutesSinceIso` ze store (callery `/cards`/`/pet` bez zmian), usunięto 2 kopie (−4 linie netto, 0 zmian zachowania, potwierdzone typecheck+suite); rygiel anty-redup [`cooldown-dry.test.ts`](../bot/src/economy/cooldown-dry.test.ts) (2× — `===` ta sama referencja; przywrócenie lokalnej kopii rozjeżdża referencję i zwala) ✅ **v0.381.0** → suite **49 plików / 423 testy**. **Rygiel twardych limitów embeda** ([`richMessage.limits.test.ts`](../bot/src/lib/richMessage.limits.test.ts) — 6× uzupełnia richMessage.test, który nie pokrywał przycinania długości): przekroczenie KTÓREGOKOLWIEK limitu Discorda = API odrzuca CAŁĄ wiadomość (custom-command/welcome/embed pada); zaryglowane `content 2000`, `title 256`, `description 4096`, `author.name 256`, `footer 2048`, `field.name 256`, `field.value 1024`, **max 25 pól** + kolejność (przycięcie PO podstawieniu zmiennych — długi `{var}` też przycięty); mutacja usuwająca `.slice(0,256)` z title zwala limit+podstawienie, usunięcie `.slice(0,25)` pól zwala kap; 0 zmian produkcyjnych ✅ **v0.382.0** → suite **50 plików / 429 testów**. **Sweep `console.*`→logger — 🏁 KONIEC** (drugi tor, mechaniczny): batch 1 = 7 feedów/pollerów (24 wyw.) ✅ **v0.339.0** + batch 2 = reszta bota (62 pliki / 154 wyw.: komendy/security/economy/live/analytics/cloud/tickets/community/top-level) ✅ **v0.340.0** ⇒ **0 `console.*` w `bot/src`** — cały bot na strukturalnym [`log.*`](../bot/src/lib/log.mts) (JSON-lines + `LOG_LEVEL`). Przy batchu 2 wyłapany **realny fix**: 7× `log.error('tag:', err)` w `index` gubiło błąd (`err` w pozycji `fields`, pola `Error` niewyliczalne ⇒ spread `{}`) → `{ err }`. Poza tym ew. monolity/`any` (odradzane — low ROI). Reszta tail-a (podziały monolitów `panelI18n.ts` 23k — high blast-radius i18n; `any` 22×) — niska wartość / wysoki churn *(next/image odrzucone — kolizja z proxy `/api/img`; panel nie ma już `AnimatePresence`)*

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 🗂️ Mapa er → wersje (źródło: CHANGELOG)

| Era | Zakres | Co przyniosła | Status |
|:--|:--|:--|:--:|
| **Fazy 0–5** | `0.1`–`0.10` | Ingest, web, rdzeń bota, panel, chmura, OAuth, anti‑nuke, `/link`, leveling, tickety, AI, EventSub, bot 24/7 | ✅ |
| **Fazy 6–7** | `0.11`–`0.37` | „Zrób wszystko" (B1–B7) + pełna personalizacja (F1–F10): moderacja, logi, weryfikacja, modmail, sugestie, AI++, gaming, analityka, Sentry | ✅ |
| **Faza 8** | `0.46`–`0.59` | Fundament customizacji: Message Studio + 14 epików „2.0" (Tickets/Applications/Reaction‑roles/Automod/Levels/AI/Library/Notifications/Donate/Creator/Integracje) | ✅ |
| **Pozostałości + Ulepszenia** | `0.60`–`0.99` | Profil 2.0, Realtime sync, multi‑user panelu, nowy look „Obsidian/Crimson", staty, interakcje, onboarding | ✅ |
| **Architekt Serwera** | `0.100`–`0.104` | Silnik provisioningu, AI‑kreator struktury, blueprinty, dry‑run, `/undo` | ✅ |
| **i18n bota** | `0.105`–`0.113`, `0.154`–`0.157` | Fundament i18n + opisy komend + runtime‑stringi + przełącznik — **14 języków** | ✅ |
| **Etapy A–K** | `0.114`–`0.194` | Przyjazność (`/help`, `/tutorial`), fun‑pack, info/mod‑utils, Architekt++, social, **safety (Etap G)**, customization 2.0 (Etap H/I), gospodarka‑gry (Etap J: stocks/pets/cards/meme), **config per‑serwer (Etap K, C‑1…C‑27)** | ✅ |
| **i18n treści + UI** | `0.202`–`0.253` | Samouczek, asystent, „Jak to działa?" 37/37, web GameVault (+RTL), **UI panelu 39/39 + powierzchnia publiczna + RTL** | ✅ |

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## ✅ Faza 0 — Fundamenty
- [x] Kolektor Steam (Web API, 58 gier)
- [x] Kolektor PlayStation (psn‑api / NPSSO, 121 tytułów)
- [x] Kolektor GOG (lokalna baza Galaxy — opcjonalny)
- [x] Normalizacja + okładki/metadane IGDB
- [x] Web „Netflix dla gier" (hero, półki, kafelki)
- [x] Rdzeń bota (discord.js v14, `/ping`, `/library`)
- [x] Szkielet dashboardu (Next.js)

## ✅ Faza 1 — Chmura + bezpieczeństwo
- [x] Discord OAuth do panelu (sesja HMAC + middleware)
- [x] Anti‑Nuke (`/antinuke` + panel Bezpieczeństwo)
- [x] Supabase (schema + seed) — biblioteka w chmurze
- [x] Deploy Vercel (`e‑bot‑dc.vercel.app`), branch‑aliasy
- [x] GH0ST `/link` (łączenie kont)
- [x] Utwardzenie repo: branch protection na `main`, Dependabot (alerty+auto‑fix), CodeQL, CODEOWNERS
- [x] Pełna dokumentacja: README/ROADMAP/ARCHITECTURE/PHASES/CHANGELOG + **Wiki** (6 stron)

## ✅ Faza 2 — Pełny panel GH0ST
- [x] Look GH0ST (czerwień/czerń, Oswald, poświaty)
- [x] Responsywność (mobilne menu, scrolle)
- [x] Strona /live (Twitch/Kick/YT/Rumble) + auto‑odświeżanie
- [x] Ekonomia (stawki GT z `/api/bot/config`)
- [x] Profil (Discord) + przycisk „Zaproś bota"
- [x] Personalizacja bota (nazwa, avatar)
- [x] Status/aktywność (presence config) + motyw/kolor akcentu
- [x] Strony loading/error/404 w stylu GH0ST

## ✅ Faza 3 — Integracja bot↔chmura
- [x] Bot pisze heartbeat `bot_status` do Supabase (panel czyta status na żywo; offline przy zamknięciu)
- [x] Bot stosuje `bot_presence` (`setPresence`) — synchronizacja z panelu co 60 s
- [x] Bot czyta whitelistę anti‑nuke + ustawienia powiadomień z Supabase (`settings-sync` → lokalny SQLite; zmiany z bota wracają mirror‑upem)
- [x] Endpoint GH0ST `link-status` → realny status powiązania w Profilu
- [x] Endpoint zdrowia bota → `/api/health` (+ alert „bot down" przez Vercel Cron)
- [x] **Realtime sync** (zero‑dep) — natychmiastowy push panel → bot (`0.65`)

## ✅ Faza 4 — Wzrost (funkcje społeczności)
> 📋 Szczegółowy plan: [`FAZA-4-PLAN.md`](FAZA-4-PLAN.md)
- [x] Tickety — panel + bot (`/ticket`, prywatne wątki → Supabase)
- [x] Leveling / XP — panel + bot (XP czat/voice, awanse, role‑nagrody → Supabase)
- [x] Reaction roles — panel `/roles` + bot
- [x] Komendy AI — `/ai` z twardym dziennym limitem (panel `/ai` + `ai_usage`)
- [x] Webhooki EventSub (zamiast pollingu) — `stream.online`
- [x] Statystyki — strona `/stats`

## ✅ Faza 5 — Statystyki + EventSub
- [x] Strona `/stats` — wykresy zużycia AI (14 dni), top XP, tickety, biblioteka
- [x] Twitch EventSub — webhook `/api/twitch/eventsub` (HMAC + challenge) → natychmiastowe ogłoszenie live
- [x] Hosting bota 24/7 — Railway (Dockerfile, heartbeat zweryfikowany)
- [ ] **Marketplace pluginów / multi‑guild jako usługa** *(otwarte — patrz „Bieżący tor")*

## ✅ Faza 6 — „Zrób wszystko" (B1–B7)
- [x] **B1** — Powitania + autorole (`/welcome`) + Automod (`/moderation`)
- [x] **B2** — Moderacja `/mod warn|timeout|clear|warnings` + historia (`mod_cases`)
- [x] **B3** — Pickery ról/kanałów w panelu (dropdowny zamiast ID)
- [x] **B4** — Narzędzia twórcy (`/creator`): auto‑wydarzenie na live + relay klipów
- [x] **B5** — Engagement: button‑role, `/remind`, `/giveaway`, starboard, temp‑voice
- [x] **B6** — Biblioteka 2.0: lista życzeń (`/wishlist`) + ręczne dodawanie gier z IGDB
- [x] **B7** — Infra/jakość: Vitest+CI, alert „bot down", handlery błędów, cache TTL
- [x] **Playwright E2E** (proxy/login/`/p/*`/health)

## ✅ Faza 7 — „Wszystko + pełna personalizacja" (F1–F10)
- [x] **F1** — Centrum sterowania (`/modules`) + MessageEditor + ColorField/GradientField + własny HEX akcentu
- [x] **F2** — Karty rang `/rank` (canvas) + baner powitalny + panel `/appearance`
- [x] **F3** — Ekonomia serwera `/eco` (daily/work/rob/pay/bank/gamble/slots/shop/buy/top) + sklep ról
- [x] **F4** — Leveling++ (mnożniki XP, no‑XP, anti‑AFK voice, custom level‑up, stack ról, `/prestige`)
- [x] **F5** — Tickety++ (`/ticketpanel`, transkrypty HTML→log+DM, oceny ⭐)
- [x] **F6** — Bezpieczeństwo++: kary+sprawy (`/case`, `temp_bans`), logi serwera (`/logging`), weryfikacja (`/verifypanel`) + anti‑raid, modmail
- [x] **F7** — Społeczność: sugestie (`/suggest`) + ankiety (`/poll`), komendy własne + autoresponder, urodziny/AFK/highlighty, liczniki kanałów
- [x] **F8** — AI++: `/tldr` + `/translate` + `lib/ai.mts`, czat z pamięcią + `/imagine`, AI‑moderacja
- [x] **F9** — Gaming unikat: free‑games feed (Epic), patch‑notes (Steam), backlog (`/backlog`), donejty Ko‑fi + price‑tracking ITAD
- [x] **F10** — Analityka + Infra: wykresy aktywności, sezonowe rankingi (`/hof`), Sentry (zero‑dep, DSN‑gated)

## ✅ Faza 8 — Fundament customizacji + 14 epików „2.0" (`0.46`–`0.59`)
- [x] **Inline toggle modułów** (`ModuleBar`; źródło prawdy = Centrum sterowania)
- [x] **Message Studio** — uniwersalny edytor embed+tekst (live‑preview 1:1, smallcaps/fonty Unicode, emoji + serwerowe, szablony, **Components V2**)
- [x] **Smallcaps** w edytorze i UI · **Compact UI**
- [x] Epiki 2.0: ✅ Tickets · ✅ Applications · ✅ Reaction roles · ✅ Automod/Security · ✅ Creator+Social · ✅ Notifications · ✅ Donate · ✅ Levels · ✅ AI (`/ask`+`/rewrite`) · ✅ Game Library (klikalne gry) · ✅ Integracje (generic incoming webhook)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## ✅ Po Fazie 8 — rozbudowa ciągła (`0.60`–`0.222`)

### 🧱 Pozostałości + Ulepszenia (`0.60`–`0.99`)
- [x] Profil 2.0 + Smallcaps w UI · free‑games multi‑store (ITAD) · hartowanie (testy, parser RSS)
- [x] Realtime panel→bot (zero‑dep) · no‑code komendy slash · uprawnienia panelu (admin/editor/viewer, multi‑user)
- [x] Nowy look „Obsidian / Crimson" + mikro‑interakcje + ekran logowania · tryb focus · glass topbar
- [x] Statystyki: trendy 14 dni, wykresy area SVG · automod (eskalacja, anty‑scam/PII) · liczniki Twitch/Kick/YouTube
- [x] Interakcje: `/trivia` `/rep` `/confess` `/xpevent` `/event` (RSVP) · onboarding (Diagnostyka + kreator startowy) · historia ekonomii na profilu

### 🏗️ Architekt Serwera (`0.100`–`0.104`)
- [x] Silnik provisioningu (twórz + auto‑wpinanie) · AI‑kreator (opis → blueprint) · blueprinty + eksport/import · podgląd (dry‑run) · `/undo` (rollback)

### 🌍 i18n bota — 14 języków (`0.105`–`0.113`, `0.154`–`0.157`)
- [x] Fundament i18n + opisy komend (Discord‑localizations) · runtime‑stringi (~40 słowników) · przełącznik języka w panelu · błędy bota i cała ekonomia/moderacja/profil na `t()`

### 🅰️ Etapy A–K (`0.114`–`0.194`)
- [x] **A „Przyjazność"** — `/help` (hub), opisy „co/po co" na stronach, `/tutorial`
- [x] **B** — 3 tryby dashboardu (Prosty/Zaawansowany/Developer)
- [x] **C** — fun‑pack (`/rps /flip /dadjoke /cat /dog`), `/sticky`, Polls v2, `/farewell`, `/search`, `/persona`, pakiet info (`/avatar /userinfo /serverinfo`), mod‑utils (`/slowmode /lock /unlock`)
- [x] **D** — `/healthcheck`, `/roleperms`, `/rolecopy`, `/blueprint`, `/aiserver`, `/undo`
- [x] **E/F** — osiągnięcia‑tiery, reaction‑roles „wybierz jedną", social pack (`/ship /hug /kiss /slap /pat`), `/marry`, gry (`/eco crime/highlow`, `/ttt`), mosty eko + `/math`
- [x] **G (SAFETY)** — `/raidmode`, `/backup` (snapshot+restore), `/heat` (adaptacyjny anty‑spam), bypass‑guard + weryfikacja hasłem, `/panic`
- [x] **H** — TempVoice 2.0, `/imageonly`, context‑menu (PPM), formularz przed ticketem, Custom Commands 2.0 (akcje+warunki)
- [x] **I** — Pulpit 2.0 (health‑score), tooltipsy/pola wg trybu, Twitch Schedule→Events, Discord AutoMod natywny, Components V2, live‑rola/vanity‑rola, **i18n panelu (nawigacja + paleta ⌘K)**
- [x] **J** — `/stocks` (giełda), role czasowe w sklepie, `/pet`, `/cards`, `/meme`
- [x] **K „Przyjazność 2.0" + config per‑serwer** — przełącznik serwerów, „Jak to działa?" na każdej stronie, asystent AI panelu, tryby 2.0, oraz **C‑1…C‑27: każdy moduł konfigurowalny per‑serwer** (powitania, leveling, sugestie, urodziny, liczniki, ekonomia, automod, logi, weryfikacja, modmail, aplikacje, tickety, TempVoice, starboard, autoresponder, liczenie/AFK, highlighty, automatyzacje, role‑menu, invite‑tracker, rankcard, heat, anti‑raid, anti‑nuke, komendy własne, digest, sezony)

### 🎁 Funkcje końcowe + i18n treści (`0.195`–`0.222`)
- [x] Wyszukiwarka komend w `/help` · automod anty‑caps/anty‑spoiler · krzywa XP (presety) · 8 motywów kart · giveaway (kasa+XP) · onboarding DM właściciela · transkrypty ticketów na web
- [x] i18n treści: samouczek, asystent AI, **„Jak to działa?" 37/37 stron × 14 jęz.**, web GameVault (+RTL +przełącznik)
- [x] **i18n UI panelu** — 39/39 stron ✅ *(patrz „Bieżący tor" u góry)*

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
<div align="center"><sub>Ostatnia aktualizacja: 2026‑06‑22 · v0.382.0 (#452) · powiązane: <a href="ROADMAP.md">ROADMAP</a> · <a href="../CHANGELOG.md">CHANGELOG</a> · weryfikacja sync: <code>pnpm docs:check</code></sub></div>
